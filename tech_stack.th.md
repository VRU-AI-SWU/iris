# Iris — เทคโนโลยีที่ใช้

<!-- lang-switch -->
[English](tech_stack.md) · **ภาษาไทย**

> เขียนใหม่ 2026-08-27 สำหรับการเปลี่ยนทิศทางมาใช้มาตรฐานแห่งชาติ stack เดิม
> (Rust/Axum + Apalis + sidecar HDBSCAN หรือก่อนหน้านั้น FastAPI + Celery + ไฟล์ JSON)
> สร้างขึ้นรอบการดึงข้อมูลจากเว็บหางานและการจัดกลุ่มคลังทักษะที่เกิดขึ้นเอง ซึ่งไม่มีอยู่แล้วทั้งคู่
> ดู [`solution-proposal.th.md`](03-solution-design/solution-proposal.th.md)

---

## รูปร่างของระบบ

สองส่วนที่ deploy แยกกัน โดยมีสัญญาเดียวเชื่อมกัน

| | เอนจิน | เว็บ |
|---|---|---|
| **รันบน** | `gpu-linux-server` — ห้องทำงานภาควิชา 24/7 | Cloudflare Workers |
| **ภาษา** | Python 3.12 | TypeScript |
| **ทำไมอยู่ตรงนั้น** | ต้องใช้ GPU ไลบรารีเนทีฟ และเวลาหลายสิบนาทีต่อรอบ | ต้องออนไลน์ไม่ว่าเอนจินจะเป็นอย่างไร |
| **เข้าถึงผ่าน** | Cloudflare Tunnel หลัง Cloudflare Access | `vru-ai.com/iris` |

เอนจิน **เผยแพร่** เอกสารผลลัพธ์ที่มีเลขเวอร์ชัน ชั้นเว็บอ่านผลที่เผยแพร่แล้วอย่างเดียว
ไม่มีอะไรบนเว็บสาธารณะที่แตะเครื่อง GPU ตอนมีคนเข้าใช้

---

## เอนจิน — Python

| ประเด็น | ตัวเลือก | หมายเหตุ |
|---|---|---|
| ภาษา | Python 3.12 | ทั้งงาน — การอ่าน PDF, NLP ภาษาไทย, การคำนวณ, เครื่องมือประเมินผล — เป็นงานที่ Python ทำได้ในตัว |
| API | FastAPI | อัปโหลด สถานะงาน ผลลัพธ์ การเผยแพร่ |
| การรันงาน | ตารางงานในฐานข้อมูล + โพรเซสคนงาน | งานใช้เวลาหลายนาทีและมีไม่มาก Celery/Redis จะเป็นเครื่องจักรที่ไม่มีจุดประสงค์ |
| ฐานข้อมูล | PostgreSQL 16 | แหล่งความจริง **ไม่ใช้ `pgvector`** — ดูด้านล่าง |
| การย้ายสคีมา | Alembic | |
| ข้อความจาก PDF | PyMuPDF | ตรวจเทียบกับ poppler บนเอกสารทดสอบทั้งสองฉบับ ผลเหมือนกัน ใช้ตัวไหนก็ได้ เลือก PyMuPDF เพราะ API สมบูรณ์กว่า |
| ดัชนีเอกสาร | [PageIndex](https://github.com/VectifyAI/PageIndex) | ระบุตำแหน่งส่วนต่าง ๆ ของ มคอ.2 ข้ามมหาวิทยาลัย โหนดพกช่วงหน้ามาด้วย ทำให้สืบย้อนได้ |
| NLP ภาษาไทย | PyThaiNLP | การตัดคำ และการฟื้น `ำ` ที่ยุบด้วยพจนานุกรม |
| ทางเลือกสำรองด้วยภาพ | Typhoon OCR (3B) | สำหรับชั้นข้อความที่สูญข้อมูลจริง ไม่ใช่แค่ถูกแทนที่ ทดสอบบนแบบฟอร์มราชการไทยที่ระยะ Levenshtein 0.04 โฮสต์เองได้ ผลลัพธ์ติดธงว่ามาจากภาพ |
| การจัดการ OCR *(กำลังประเมิน)* | [xberg](https://github.com/xberg-io/xberg) | แกน Rust สัญญาอนุญาต MIT มี Python binding รองรับ OCR เจ็ดตัวรวม `vlm` แบบทั่วไป มีโซ่สำรอง เกณฑ์ความมั่นใจ และโมเดลตาราง TATR/SLANet ⚠️ `quality_score` ของมันมองไม่เห็นความเสียหายของวรรณยุกต์ไทย ตัววินิจฉัยของ Iris จึงยังเป็นด่าน |
| การคำนวณ | NumPy, SciPy | เมทริกซ์การค้นคืน ตัวชี้วัดการจับคู่ |
| รายงาน | Jinja2 + WeasyPrint | รายงาน HTML ส่งออกเป็น PDF |
| ผลลัพธ์ LLM ที่มีโครงสร้าง | Pydantic v2 | ผลการเชื่อมทักษะถูกจำกัดให้เป็นรหัสทักษะที่ถูกต้องเท่านั้น |
| การทดสอบ | pytest | ฟังก์ชันตัวชี้วัดมีการทดสอบแบบรู้ข้อมูลเข้า-ข้อมูลออก |
| ตรวจ / จัดรูปแบบโค้ด | ruff | |

### ทำไมไม่ใช้ฐานข้อมูลเวกเตอร์

คลังทักษะเป็น **ข้อมูลอ้างอิงระดับชาติที่คงที่** จำนวน 4,376 รายการ นั่นคือเมทริกซ์ `float32`
ขนาด **13–18 MB** ขึ้นกับมิติของโมเดลฝังตัว (13 MB ที่ 768 มิติ, 18 MB ที่ 1,024)
การคำนวณความคล้ายโคไซน์แบบตรงบนเมทริกซ์นี้คือการคูณเมทริกซ์ `numpy` ครั้งเดียว —
ใช้เวลาระดับไมโครวินาที ไม่ต้องสร้างดัชนี ไม่ต้องรันบริการเพิ่ม ไม่ต้องปรับค่าประมาณ

`pgvector` อยู่ในการออกแบบเดิมเพื่อเก็บคลังทักษะ *ที่เกิดขึ้นเอง* ซึ่งโตขึ้นเมื่อมีหลักสูตรใหม่
เข้ามาจัดกลุ่ม การเปลี่ยนทิศทางลบคลังนั้นทิ้ง ปัญหาการขยายขนาดที่ฐานข้อมูลเวกเตอร์แก้จึงไม่มีอยู่

การค้นคืนรวมเมทริกซ์หนาแน่นนั้นเข้ากับการจับคู่เชิงคำศัพท์ ซึ่งจำเป็นเพราะชื่อเครื่องมือ
(`Docker`, `.NET Core`, `Apache Spark`) จับคู่ด้วยคำได้ดีกว่าด้วยความหมายมาก

### แบบจำลอง

ให้บริการผ่าน endpoint ที่เข้ากันได้กับ OpenAI เพื่อให้สภาพแวดล้อมพัฒนากับใช้งานจริง
ต่างกันแค่ `MODEL_SERVER_URL`

### ผู้ให้บริการแบบจำลอง

การฝังตัวและการตัดสินอยู่หลัง **ส่วนต่อประสานเดียวที่ไม่รู้จักผู้ให้บริการ** โดยมีสองแบ็กเอนด์ —
รูปแบบที่โครงการ Argus ของแล็บใช้งานจริงอยู่แล้ว (`server/llm.ts`)

| | ในเครื่อง | Cloudflare Workers AI |
|---|---|---|
| Endpoint | เข้ากันได้กับ OpenAI, Ollama บน `gpu-linux-server` | แคตตาล็อกโมเดล `@cf/…` |
| ค่าใช้จ่าย | ฟรี แต่แย่งทรัพยากรกับงาน GPU อื่นของแล็บ | **ฟรี 10,000 neurons/วัน** ≈ $0.11 |
| ค่าใช้จ่ายของ Iris | — | **≈ $0.036 ต่อหลักสูตร** ⇒ ราว 3 หลักสูตร/วัน ในโควตาฟรี |
| ความพร้อมใช้ | VRAM ราว 15 GB เมื่อโปรเจกต์อื่นกำลัง train | ไม่ได้รับผลจากการแย่ง GPU |

Iris วิเคราะห์หลักสูตรรวมกันไม่กี่หลักสูตร โควตาฟรีจึงเหลือเฟือ — ตรงข้ามกับ Argus
ที่เดโมเชิงโต้ตอบต้องใช้ราว 32 เคส/วัน และเคยใช้โควตาหมด เอกสาร มคอ. เป็นเอกสารสาธารณะ
จึงไม่มีเหตุผลด้านถิ่นที่อยู่ของข้อมูลที่จะต้องเลือกรันในเครื่องอย่าง Argus

**ใช้ `server/llm.ts` ของ Argus ซ้ำ แทนที่จะเขียนใหม่** โค้ดนั้นแยก 429 ที่เกิดจากโควตาหมด
(`"code":4006` ห้าม retry เด็ดขาด) ออกจาก 429 ที่เกิดจากความจุ (retry พร้อม backoff
โดยเคารพ `Retry-After`) ไว้แล้ว หลังฟังก์ชัน `chat()` เดียวที่ไม่รู้จักผู้ให้บริการ

🔴 **ผู้ให้บริการถูกตรึงต่อรอบการวิเคราะห์ ไม่มีการสลับกลางรอบ** หลักสูตรที่เชื่อมทักษะด้วยสองโมเดล
ไม่ใช่การวิเคราะห์ที่ทำซ้ำได้ เมื่อโควตาหมด รอบนั้นล้มเหลวและเข้าคิวใหม่บนผู้ให้บริการอีกรายตั้งแต่ต้น
ตาราง `analysis_run` บันทึกผู้ให้บริการ โมเดล และวันที่ของสแนปช็อต

**การนำเข้าเอกสารย้ายไม่ได้** PyMuPDF, PageIndex และ Typhoon OCR ไม่มีสิ่งเทียบเท่าบน Workers

| บทบาท | ข้อกำหนด |
|---|---|
| การตัดสิน | ทำตามคำสั่งแบบเลือกตอบที่มีข้อจำกัด และคืนค่า JSON ที่ถูกต้อง RAG ลดงานจากการสร้างข้อความอิสระเหลือการเลือกจากตัวเลือกราว 30 รายการ โมเดลที่พอดีกับ VRAM ที่มีจึงใช้ได้ |
| การฝังตัว | รองรับหลายภาษา จัดการคำศัพท์ทักษะทั้งไทยและอังกฤษได้ |
| การให้บริการ | LM Studio (พัฒนา) / Ollama (ใช้งานจริง) ทั้งคู่บน `gpu-linux-server` |

### เครื่องแม่ข่าย — วัดเมื่อ 2026-08-28

| | |
|---|---|
| GPU | **NVIDIA GeForce RTX 3090, 24 GB** · ไดรเวอร์ 580.173.02 |
| VRAM ว่างขณะวัด | **15.4 GB** — ราว 9 GB ถูกยึดโดยสิ่งอื่นที่ไม่ใช่ Ollama |
| Ollama | รันเป็นบริการ ดึง `iris-adjudicator` และ `llama3.1-typhoon2-8b` มาแล้วเมื่อ 2026-08-31 |
| พื้นที่ดิสก์ว่าง | 193 GB |
| Python | 3.12.3 — ตรวจสอบเอนจินกับรุ่นนี้แล้ว |

**9 GB นั้นไปไหน** (วัดเมื่อ 2026-08-28): งาน train Prostate MRI ยึดไว้ **7,794 MiB**
(`/opt/conda/bin/python`) และ Xorg, Cinnamon, เบราว์เซอร์, Steam กับเทอร์มินัลยึดอีก
**ราว 1,190 MiB อย่างถาวร** ไม่มีอะไรค้างหรือรั่ว

⚠️ **การแย่งทรัพยากรกินเวลาหลายวัน** โปรเจกต์นั้น train แบบ 5-fold cross-validation
ที่ราว 30 ชั่วโมงต่อ fold รวมแล้วราว **150 ชั่วโมงต่อหนึ่งรอบเต็ม** แต่ละ fold ถูกสั่งรัน
ทีละอันโดยตั้งใจ เพื่อให้สองโปรเจกต์สลับกันได้ ช่วงเวลาที่พึ่งพาได้สำหรับงานที่ต้องใช้การ์ดทั้งใบ
จึงอยู่ *ระหว่าง fold* ไม่ใช่ "อีกสองวัน" ให้วางแผนงาน GPU ของ Iris รอบข้อเท็จจริงนี้ ไม่ใช่รอ

⚠️ **วางแผนสำหรับกรณีที่มีการแย่ง ไม่ใช่กรณีที่ว่าง** เครื่องนี้ใช้ร่วมกับโปรเจกต์อื่นของแล็บ
ตัวเลข ~15 GB จึงเป็นตัวเลขที่การวิเคราะห์ต้องอยู่รอดให้ได้ ส่วน ~23 GB คือสิ่งที่มีเมื่อไม่มีอะไร
กำลัง train การออกแบบให้พอดี 23 GB จะทำให้การวิเคราะห์ล้มเหลวอย่างคาดเดาไม่ได้ทุกครั้งที่
โปรเจกต์อื่นเริ่มรัน

มาตรการบรรเทา ทั้งหมดต้นทุนต่ำ

- **กำหนดขนาด context ไม่ใช่แค่ขนาดโมเดล** วัดเมื่อ 2026-08-31: โมเดล 8B *ตัวเดียวกัน*
  ใช้ **10.0 GB ที่ context ปริยาย 32,768 ของ Ollama และ 6.6 GB ที่ 8,192** ขณะที่ prompt
  ของ Iris ยาวราว 2,650 โทเคน context ไม่ใช่จำนวนพารามิเตอร์คือสิ่งที่ครองหน่วยความจำ
  และ 3.4 GB ที่ประหยัดได้คือส่วนต่างระหว่างการอยู่ร่วมกับ training ของอีกโปรเจกต์กับการต้องรอคิว
- **กำหนด `OLLAMA_KEEP_ALIVE` อย่างชัดแจ้ง** ตอนนี้ยังไม่ได้ตั้ง จึงใช้ค่าปริยาย 5 นาทีของ Ollama
  ซึ่งบังเอิญเป็นพฤติกรรมที่ถูกต้องภายใต้การแย่งทรัพยากร — โมเดลคืน VRAM ระหว่างรอบ —
  แต่ควรเป็นการตัดสินใจ ไม่ใช่ความบังเอิญ
- **ตรวจ VRAM ว่างก่อนเริ่มรอบ แล้วเข้าคิวแทนที่จะล้มเหลว** ตารางงานมีอยู่แล้วสำหรับเรื่องนี้
- **การนำเข้าเอกสารไม่ต้องใช้ GPU** Sprint 1–2 — ด่านตรวจความสมบูรณ์ การซ่อมกลิฟ
  การนำทางด้วย PageIndex และการสกัดส่วนต่าง ๆ — เป็นงาน CPU และทำได้ขณะการ์ดไม่ว่าง

### แบบจำลอง — ตัดสินแล้ว 2026-08-31

**`iris-adjudicator`** — `qwen3:8b` ที่ลงทะเบียนใหม่ผ่าน Modelfile ด้วย `num_ctx 8192`
และ `temperature 0` เลือกจากพฤติกรรมการเชื่อมทักษะที่วัดได้เทียบกับ `llama3.1-typhoon2-8b`
ไม่ใช่จากขนาด ใช้หน่วยความจำ 6.6 GB ราว 4.2 วินาทีต่อรายวิชา

ตั้งค่า context ที่ฝั่งเซิร์ฟเวอร์แทนที่จะส่งมากับแต่ละคำขอ ด้วยสองเหตุผล: API ที่เข้ากันได้กับ
OpenAI ไม่มีฟิลด์ `num_ctx` และเอนจินต้องพูดภาษานั้นต่อไป เพื่อให้สภาพแวดล้อมพัฒนากับใช้งานจริง
ยังคงต่างกันแค่ `MODEL_SERVER_URL`

🔴 **แบบจำลองที่คิดก่อนตอบต้องถูกสั่งไม่ให้คิด** `qwen3` ใช้โควตาโทเคนไปกับฟิลด์ `reasoning`
ที่รูปแบบคำตอบของ OpenAI ไม่ได้คืนมาเป็นเนื้อหา คำตอบจึงมาถึงเป็น **สตริงว่าง** พร้อม
`finish_reason: length` การตั้ง `reasoning_effort: "none"` แก้ได้ ส่วน `think: false` และ
`chat_template_kwargs.enable_thinking` ถูก Ollama เพิกเฉยอย่างเงียบ ๆ เอนจินถือว่าคำตอบ
ที่ถูกตัดกลางคันเป็น **ความล้มเหลว** ไม่ใช่รายวิชาที่ไม่มีทักษะ

---

## เว็บ — Astro บน Cloudflare

| ประเด็น | ตัวเลือก | หมายเหตุ |
|---|---|---|
| เฟรมเวิร์ก | Astro | เน้น static ก่อน พร้อม island สำหรับมุมมองเชิงโต้ตอบ ตรงกับวิธีที่ `vru-ai-web` deploy อยู่แล้ว |
| โฮสติ้ง | Cloudflare Workers (ไฟล์ static) | รูปแบบเดียวกับเว็บแล็บ |
| เส้นทาง | `vru-ai.com/iris` | อยู่ในรายการโครงการของแล็บ |
| การยืนยันตัวตน | Cloudflare Access | รายชื่ออาจารย์ในภาควิชา ไม่มีโค้ดล็อกอินในแอปพลิเคชัน |
| การเข้าถึงเอนจิน | Cloudflare Tunnel | ออกทางเดียวจากเซิร์ฟเวอร์ในออฟฟิศ ไม่ต้อง forward port ไม่ต้องมี public IP ไม่ต้องแก้ไฟร์วอลล์มหาวิทยาลัย |
| แผนภูมิ | island ฝั่งไคลเอนต์ | แผนภาพความร้อน (รายวิชา × ทักษะ) ตารางช่องว่างที่จัดอันดับ |
| ผลลัพธ์ที่เผยแพร่ | **JSON ที่สร้างตอน build** | ไม่ใช่ D1/R2 ในรุ่นแรก — มีไม่กี่หลักสูตร เก็บเวอร์ชันใน git ทำซ้ำได้ ย้ายไป D1/R2 เมื่อชุดผลลัพธ์โตเกินไฟล์ build |

### พื้นผิวสาธารณะและพื้นผิวที่จำกัดสิทธิ์

```
vru-ai.com/iris          สาธารณะ · Astro static · ผลที่เผยแพร่แล้ว · ไม่มี backend
vru-ai.com/iris/app      จำกัดสิทธิ์ · Cloudflare Access → Tunnel → FastAPI
```

พื้นผิวสาธารณะไม่ได้พึ่งพาเอนจินตอนทำงาน จึงไม่ได้รับผลกระทบเมื่อเอนจินไม่ว่าง กำลังรีสตาร์ต
หรือออฟไลน์

---

## การจัดเก็บข้อมูล

| อะไร | ที่ไหน | commit หรือไม่ |
|---|---|---|
| สแนปช็อตมาตรฐานแห่งชาติ | `data/skillmapping/<YYYY-MM-DD>/` | **ใช่** — เป็นหลักยึดของการทำซ้ำ |
| ไฟล์ PDF มคอ. ต้นทาง | `data/programmes/` | ไม่ — เป็นเอกสารของสถาบัน |
| ข้อมูลระหว่างทำงานของเอนจิน | PostgreSQL | ไม่ |
| ผลลัพธ์ที่เผยแพร่ | JSON ที่มีเวอร์ชัน สร้างเข้าไปในชั้นเว็บ | **ใช่** |
| รายงาน PDF ที่สร้างขึ้น | ระบบไฟล์ของเอนจิน ย้ายไป R2 เมื่อโตเกิน | ไม่ |

สแนปช็อตถูกตรึงไว้โดยตั้งใจ: API ต้นทางยังเป็นเบต้า (`0.8.1-beta-public`) และข้อมูลจะเปลี่ยน
**เอนจินไม่เคยเรียก API สดระหว่างการวิเคราะห์** — อ่านจากสแนปช็อตที่ตรึงไว้ ผลลัพธ์จึงทำซ้ำได้
แม้ผ่านไปหลายปี

---

## โครงสร้าง repository

```
iris/
├── 01-brainstorm/
├── 02-literature-review/
├── 03-solution-design/
│   ├── solution-proposal.md
│   ├── product-design.md
│   └── data-feasibility.md
├── 04-implementation/
│   ├── engine/                 Python — นำเข้า เชื่อมทักษะ วิเคราะห์ API
│   ├── annotation/             คู่มือผู้ประเมินสำหรับด่าน Sprint 4
│   └── web/                    Astro — เว็บสาธารณะ + แอปที่จำกัดสิทธิ์
├── 05-reports/
└── data/skillmapping/          สแนปช็อตมาตรฐานแห่งชาติที่ตรึงไว้
```

---

## ตัวแปรสภาพแวดล้อม

```
# เซิร์ฟเวอร์แบบจำลอง
MODEL_SERVER_URL=http://localhost:1234/v1     # LM Studio (พัฒนา)
# MODEL_SERVER_URL=http://ollama:11434/v1     # Ollama (ใช้งานจริง)
IRIS_MODEL=iris-adjudicator                   # qwen3:8b ที่ num_ctx 8192
IRIS_REASONING_EFFORT=none                    # จำเป็นสำหรับแบบจำลองที่คิดก่อนตอบ
IRIS_PROVIDER=local                           # หรือ workers-ai ตรึงต่อรอบ
EMBEDDING_MODEL=<ยังไม่กำหนด — การค้นคืนเชิงหนาแน่นมาพร้อม ablation ของ Sprint 4>

# ฐานข้อมูล
DATABASE_URL=postgresql://iris:...@localhost:5432/iris

# สแนปช็อตมาตรฐาน
SKILLMAP_SNAPSHOT=data/skillmapping/2026-08-27

# PageIndex
PAGEINDEX_MODE=local
```

---

## บันทึกการตัดสินใจ

| การตัดสินใจ | เหตุผล |
|---|---|
| ใช้ Python ทั้งหมดในเอนจิน | การอ่าน PDF, NLP ภาษาไทย, การคำนวณ และเครื่องมือประเมินผล ล้วนเป็น Python ข้อได้เปรียบของ Rust คือการดึงข้อมูลแบบขนาน ซึ่งการเปลี่ยนทิศทางตัดออกไปแล้ว แกน Rust จะต้องมี sidecar Python อยู่ดี |
| ไม่ใช้ฐานข้อมูลเวกเตอร์ | คลังคงที่ 4,376 รายการอยู่ในหน่วยความจำได้ การค้นแบบตรงดีกว่าการค้นแบบประมาณที่ขนาดนี้ |
| ไม่ใช้ Celery/Redis | งานมีน้อย ใช้เวลานาน และไม่ทำพร้อมกันมาก ตารางงานกับโพรเซสคนงานดูแลง่ายกว่าและทำซ้ำง่ายกว่า |
| ใช้ PageIndex สำหรับการนำทางในเอกสาร | โครงสร้าง มคอ.2 ถูกกำกับด้วยระเบียบ แต่การจัดรูปแบบไม่ถูกกำกับ และการสืบย้อนถึงช่วงหน้าเป็นข้อกำหนดของงานวิจัย |
| ตัวสกัดข้อความไม่ใช่ตัวแปรที่ปรับได้ | poppler, PyMuPDF และ xberg คืนความเสียหายเหมือนกันทุกไบต์บนเอกสาร มศว (134.5 เครื่องหมายต่อ 1,000 อักขระไทย เท่ากันทั้งสาม) ข้อบกพร่องอยู่ที่ `ToUnicode` ที่หายไปในไฟล์ PDF ไม่มีตัวอ่านใดกู้คืนได้ การซ่อมและการสำรองด้วยภาพจึงเป็นทางเดียว |
| ซ่อมกลิฟแบบกำหนดได้ **ก่อน** แล้วค่อยสำรองด้วยภาพ | เมื่อความเสียหายเป็นการแทนที่ การซ่อมนั้นแม่นยำ ตรวจสอบย้อนได้ และไม่มีค่าใช้จ่าย เมื่อชั้นข้อความสูญข้อมูลจริง (การยุบ `ำ` ของ ม.เกษตร) ไม่มีตารางใดกู้คืนได้ VLM ภาษาไทยขนาด 3B ที่โฮสต์เองจึงเป็นทางสำรอง พร้อมติดธงที่มา |
| แยกเป็นสองส่วนที่ deploy ต่างหาก | เว็บสาธารณะต้องไม่ขึ้นกับความพร้อมใช้ของเครื่อง GPU |
| ใช้ `gpu-linux-server` ของตนเอง ไม่ใช่ CSML | GPU ของ CSML ถูกแย่งใช้ทั้งภาควิชาและอยู่นอกการควบคุมของโครงการ เครื่องนี้ **ก็ใช้ร่วมกัน** — กับโปรเจกต์อื่นของแล็บ — แต่การแย่งนั้นจัดตารางกันเองได้ ซึ่งคือข้อได้เปรียบจริงที่แคบกว่า |
| Cloudflare Tunnel + Access | เข้าถึงเครื่องที่อยู่หลัง NAT ของมหาวิทยาลัยได้โดยไม่เปิดช่องขาเข้า และให้การยืนยันตัวตนโดยไม่ต้องเขียนโค้ด |
| ใช้ Astro ไม่ใช่ Next.js | เป็นงานแสดงข้อมูล ตรงกับการ deploy ที่เว็บแล็บใช้อยู่ และเหมาะกับหน้าโครงการแบบ static/SEO มากกว่า |
| ใช้ JSON ตอน build ไม่ใช่ D1/R2 ในรุ่นแรก | ชุดผลลัพธ์เล็ก เก็บเวอร์ชันใน git ลดชิ้นส่วนที่ต้องดูแลไปหนึ่งชิ้น |
| ตรึงสแนปช็อตและ commit ไว้ | ต้นทางยังเป็นเบต้าและจะเปลี่ยน การทำซ้ำได้เป็นข้อกำหนดของการตีพิมพ์ |
